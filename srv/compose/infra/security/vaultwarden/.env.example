# Vaultwarden
# Generate with: openssl rand -base64 48
VW_ADMIN_TOKEN=

# Optional: set if you want signups on for first-run only
# Recommended: keep false and create users manually (or temporarily enable)
VW_SIGNUPS_ALLOWED=false
