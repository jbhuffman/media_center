services:  
  uptime-kuma:
    image: louislam/uptime-kuma:2.0.2
    container_name: infra_monitoring_uptime-kuma
    environment:
      - TZ=${TZ}
    volumes:
      - /srv/appdata/infra/monitoring/uptime-kuma/data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - 3001:3001
    dns:
      - 192.168.6.10
    restart: unless-stopped